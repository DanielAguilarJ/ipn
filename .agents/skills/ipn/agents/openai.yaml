interface:
  display_name: "Ipn"
  short_description: "Repo-specific patterns and workflows for ipn"
  default_prompt: "Use the ipn repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true